import React from "react";
export const metadata = {
  title: "Blog | Restaurent",
  description: "Best food, Healthy body, Freash mind ",
};

const BlogPage = () => {
  return <section>BlogPage</section>;
};

export default BlogPage;
